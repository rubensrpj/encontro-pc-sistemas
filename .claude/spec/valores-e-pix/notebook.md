# Notebook — main_valores-e-pix

> What surfaced during this work and does NOT belong to `valores-e-pix`. What belongs
> to the spec amends the spec; this file is everything else, and once the pull
> request opens it is the next cycle's prompt.

- O .claude/.gitignore escrito pelo upsert nao cobre .claude/.session/, entao os logs de evento da harness entram em todo commit sob a lei do add -A: 53 arquivos ndjson neste PR, 79 ja versionados antes. Corrigir no template do proprio mustard (o binario e o unico escritor do arquivo), nunca a mao.
